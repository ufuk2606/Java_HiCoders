package School;

public class School {
    private Sinif[] sinifs = new Sinif[5];

    public Sinif[] getClasses() {
        return sinifs;
    }

    public void setClasses(Sinif[] sinifs) {
        this.sinifs = sinifs;
    }
}
